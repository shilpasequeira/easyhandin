<%= form_for(@course) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :org_name do %>Organization
    <%= f.select(:org_name, user_organizations, {disabled: @course.is_published?}) %>
  <% end %>
  <%= f.submit "Save", class: "button tiny" %>
<% end %>
